runtime: python311

entrypoint: uvicorn main:app --host 0.0.0.0 --port $PORT

env_variables:
  DB_HOST: "34.58.98.217"
  DB_PORT: "5432"
  DB_NAME: "cloudstore"
  DB_USER: "cloud-store-dbp"
  DB_PASSWORD: "CloudStore2026"
  BUCKET_NAME: "cloud-store-jesusalex-ufps.appspot.com"